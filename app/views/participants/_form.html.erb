<%= form_for @participant, :html => { :class => 'form-horizontal' } do |f| %>
  
  <%= f.hidden_field :worker_id %>
  <%= f.hidden_field :hit_id %>
  <%= f.hidden_field :assignment_id %>
  <%= f.hidden_field :ip_addr %>
  <%= f.hidden_field :cond %>

  <div class="container">

  <br>

  <p>

  You have been invited to participate in this research study to help us understand why people perform work on mechanical turk. Your participation would take about 15 minutes. You are free to withdraw your consent and to discontinue participation at any time. You must be 18 or older to participate. 


  </p>

  </div>

  <div class="form-actions">

  <% if @participant != nil and @participant.worker_id != nil and @participant.worker_id != ""  %>

    <p>To complete the HIT, please:</p>

    <p>1. Begin and complete the survey. </p>

    <%= link_to 'Click Here for the Survey', Participant::CONDITION_URLS[@participant.cond], :target => '_blank', :style=>"font-size:20px;" %>

    <br><br><br>

    <p>2. Enter in the <b>survey code</b> you receive from completing the survey below, and then submit the HIT.
    <br>Your work will be rejected if the code does not match with the one you received from completing the HIT.</p>

    <div class="control-group">
      <%= f.label :survey_code, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :survey_code, :class => 'text_field' %>
      </div>
    </div>


    <%= f.submit "Commit HIT" , :class => 'btn btn-primary'  %>

    <br>

  <% else %>
    *You must accept HIT to continue.<br>
  <% end %>

</div>

<% end %>
